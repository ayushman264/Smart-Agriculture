<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

<script>
        $(document).ready(function(){
          $(".btn1").click(function(){
            //console.log($(this).attr('id'));
            $.ajax({
            url: "http://localhost:5000/updateSensorStatus/",
            type: "PUT",
            data: {sid:$(this).attr('id')},
            dataType: "json",
            success: function (res) {
                if(res.status===200) console.log("Status check true");
                if(res.status==="success") console.log("test 2 success");
                var ret = JSON.stringify(data);
                //window.location.href='Deleted.html';
            },
            error: function (xhr, status, error) {
                if(res.status===200) console.log("Status check truekkkkkk");
                if(res.status==="success") console.log("test 2 successkjkjkjkj");
                //window.location.href='Not Deleted.html';
            }
            }).done(()=>{
                console.log("ggggggg"); 
                location.reload(true);
            })				
            location.reload(true);
          });
        });
    </script>
<table class="table">
    <thead>
            <tr>
                <th>Edge Station Number</th>
                <th>Farmer ID</th>
                <th>Sensor Type</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
    </thead>
    <tr>
        <% if (message=="No sensors on any edge station"){ %>
        <%= message %>
        <% }else{ %>
        <% message.forEach(function(msg){ %>
        <td><%= msg.esId %></td> 
        <td><%= msg.fId %></td>  
        <td><%= msg.sType %></td> 
        <td><%= msg.status %></td>
        <td><button class="btn1" id=<%=msg.sId%>>Active</button></td>
    </tr><%  }); %>
    <% } %>

</table>

<h6><button type="button" onclick="location.href='http://localhost:5000/mcDashboard';"> View Dashboard</a></h6>